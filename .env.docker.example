APP_NAME=HA Entity Vault
SESSION_SECRET=replace-with-a-long-random-secret
SESSION_HTTPS_ONLY=false

# Docker default persistence path for SQLite mode.
HEV_DATA_DIR=/data
# Host port published by Docker Compose (container still listens on 8000).
HEV_HOST_PORT=23010
DATABASE_URL=

# Optional Postgres mode override.
# DATABASE_URL=postgresql+psycopg://hev:hev_change_me@postgres:5432/ha_entity_vault
HEV_POSTGRES_DB=ha_entity_vault
HEV_POSTGRES_USER=hev
HEV_POSTGRES_PASSWORD=hev_change_me

# Optional token override. If set, this value is preferred over token stored in SQLite
# for profiles that use token_env_var=HA_TOKEN.
HA_TOKEN=

# Optional per-profile override by environment variable name.
# Example profile token_env_var value: HA_LAB_TOKEN
HA_LAB_TOKEN=

# Optional LLM settings for suggestion/draft generation.
HEV_LLM_ENABLED=false
HEV_LLM_BASE_URL=
HEV_LLM_API_KEY=
HEV_LLM_MODEL=
HEV_LLM_TIMEOUT_SECONDS=20
HEV_LLM_MAX_CONCURRENCY=4

# Optional build SHA override used by /config update checker when .git metadata is unavailable.
HEV_BUILD_COMMIT_SHA=
# Optional compose image tag override used by update-manager deployments.
HEV_IMAGE_TAG=ha-entity-vault:local

# Host-side auto-update manager settings.
AUTO_UPDATE_ENABLED=true
AUTO_UPDATE_SCHEDULE=04:00
UPDATE_TIMEOUT_SECONDS=120
BACKUP_RETENTION_DAYS=7
UPDATE_BRANCH=main

# Advanced update-manager paths and safety controls.
AUTO_UPDATE_LOG_FILE=/var/log/ha-entity-vault-update.log
AUTO_UPDATE_BACKUP_DIR=/var/backups/ha-entity-vault
AUTO_UPDATE_STATE_DIR=/var/lib/ha-entity-vault-update
AUTO_UPDATE_LOCK_FILE=/var/lock/ha-entity-vault-update.lock
AUTO_UPDATE_MIN_FREE_MB=1024
UPDATE_POLL_SECONDS=3
UPDATE_EXPECTED_ORIGIN=https://github.com/jasonpagliaro/homeassistant-entity-helper.git
COMPOSE_SERVICE=app
SQLITE_VOLUME=hev_data
SQLITE_DB_PATH=/data/ha_entity_vault.db
POST_DEPLOY_MONITOR_SECONDS=600
POST_DEPLOY_CRASH_THRESHOLD=3

# Optional API key env vars referenced by profile-scoped LLM connections.
# The app stores only env var names for LLM credentials.
OPENAI_API_KEY=
OPENROUTER_API_KEY=
LM_STUDIO_API_KEY=

# Max entities evaluated per draft run.
HEV_AUTOMATION_DRAFT_MAX_ITEMS=50
